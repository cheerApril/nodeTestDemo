extends layout


block content
    form(
    action = "http://localhost:3000/admin/login",
    method = "post")
    p 账号
    input(
        type = `text`
        name = `account`
    )
    p 密码
    input(
        type = `password`
        name = `password`
    )
    br
    input(
        type = `submit`
        value = `登陆`
    )